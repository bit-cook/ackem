# Build resources (electron-builder)

- **`icon.png`** — 256×256+ app icon (required for NSIS). Committed placeholder uses letter **A** on dark background.
- If missing or too small, `npm run dist:green` or `npm run dist:setup` fails with NSIS icon error.
